#!/bin/sh
# Runtime environment for MoyaDB release

export RELEASE_DISTRIBUTION="${RELEASE_DISTRIBUTION:-name}"
export RELEASE_NODE="${RELEASE_NODE:-moya_db@127.0.0.1}"
export RELEASE_COOKIE="${RELEASE_COOKIE:-moya_db_cookie_change_me}"